# Nucleus

Nucleus is a local AI control plane.

The daemon is the product brain. It owns sessions, routing, auth, persistence, machine actions, and the API contracts used by every client.

The web app is a client, not the backend.
