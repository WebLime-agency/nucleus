# Clients

This directory is reserved for future Nucleus client surfaces.

Planned client types:

- macOS native client
- iOS native client
- additional operator-facing surfaces if they become necessary

The web UI under `apps/web` is the only active client right now.

